// Entry point informativo. I comandi reali sono:
//   npm run scan            -> src/bot.ts (uno scan singolo, modalità paper)
//   npm run replay:raw      -> src/replay.ts raw (baseline onesta sui dati storici)
//   npm run replay:memory   -> src/replay.ts memory (usa ledger.csv e learnings.md)
//   npm run memory:reset    -> src/resetMemory.ts (svuota la memoria)
console.log("Usa: npm run scan | npm run replay:raw | npm run replay:memory | npm run memory:reset");
